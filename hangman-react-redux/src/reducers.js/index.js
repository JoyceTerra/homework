// 4. Create at least one reducer
//  - It's a function
//  - Should return an initial state (use string so you can see it easily in devtools)
//  - Should export it

import game from './game'


//Store --> which requires an empty reducer which should be in src/reducers/index.js.
import { combineReducers } from 'redux'
game

export default combineReducers({
 
})